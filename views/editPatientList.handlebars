{{!< layouts/main}}

<section class="intro">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-12">
                <div class="card">
                    <div class="card-body">
                        <h2 class="card-title text-center mb-4">Edit Clients</h2>
                        <ul class="list-group">
                            {{#each clients}}
                            <li class="list-group-item">
                                <a href="/edit-client/{{id}}">{{name}}</a>
                            </li>
                            {{/each}}
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
