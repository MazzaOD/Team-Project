{{!< layouts/main}}

<section class="intro">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-12">
                <div class="card">
                    <div class="card-body">
                        <h2 class="card-title text-center mb-4">Choose Patient to Edit</h2>
                        <form id="editPatientForm" method="GET">
                            <div class="mb-3">
                                <label for="patientId" class="form-label">Select Patient:</label>
                                <select class="form-select" id="patientId" name="patientId" required>
                                    {{#each patients}}
                                    <option value="{{PatientNo}}">{{Name}}</option>
                                    {{/each}}
                                </select>
                            </div>
                            <button type="button" class="btn btn-primary" onclick="redirectToEditPage()">Edit Patient</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<script>
    function redirectToEditPage() {
        const selectedPatientId = document.getElementById('patientId').value;
        window.location.href = `/edit-patient/${selectedPatientId}`;
    }
</script>
