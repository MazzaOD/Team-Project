{{!< layouts/main}}

<section class="intro">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-12">
                <div class="card">
                    <div class="card-body">
                        <h2 class="card-title text-center mb-4">Confirm Deletion</h2>
                        <p>Are you sure you want to delete the dentist {{dentist.name}}?</p>
                        <form action="/delete-dentist/{{dentist.empNo}}" method="POST">
                            <button type="submit" class="btn btn-danger">Yes, Delete</button>
                            <a href="/" class="btn btn-secondary">Cancel</a>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
